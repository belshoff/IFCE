#include <bits/stdc++.h>

using namespace std;

typedef pair<int, int> pii;

const int MAXN = 15;

int n;
vector<pii> adj[MAXN];

int main() {
  while(cin >> n) 
  {
    if(n == 0) break;
  }
  return 0;
}
